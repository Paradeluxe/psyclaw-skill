name: code_probe
version: "1.0"
description: minimal probe to extract real CodeComponent XML

routines:
  - name: probe
    duration: 1.0
    components:
      - type: code
        name: my_code
        code_type: Py
        begin_routine: "msg = 'hello'"
        end_routine: "print(msg)"
        each_frame: "pass"

loops: []
stimuli: []