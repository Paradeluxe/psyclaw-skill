{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "title": "Conditions",
  "description": "A table of conditions",
  "type": "object",
  "properties": {
    "headers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 2,
      "description": "Column headers for the conditions table. First column must be 'Condition', followed by variable names starting with '$'",
      "if": {
        "minItems": 1
      },
      "then": {
        "items": [
          {
            "const": "Condition",
            "description": "First column must be 'Condition'"
          }
        ],
        "additionalItems": {
          "type": "string",
          "pattern": "^\\$[a-zA-Z_][a-zA-Z0-9_]*$",
          "description": "Subsequent columns must be variable names starting with '$' (e.g., $stimWord, $inkColor)"
        }
      }
    },
    "rows": {
      "type": "array",
      "items": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "description": "Data rows for the conditions table. Each row must have the same number of columns as headers"
    }
  },
  "required": ["headers", "rows"]
}